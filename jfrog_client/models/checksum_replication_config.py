# coding: utf-8

"""
    JFrog Platform API (Unofficial)

    **Unofficial** combined REST API spec for the JFrog Platform. This is not an official JFrog specification and may be incomplete or inaccurate.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ChecksumReplicationConfig(BaseModel):
    """
    ChecksumReplicationConfig
    """ # noqa: E501
    check_binary_existence_allowed: StrictBool = Field(description="Allows the usage of the checkBinaryExistenceInFilestore flag in the Pull/Push Replication API on the source Artifactory", alias="checkBinaryExistenceAllowed")
    days_to_trust: Optional[Annotated[int, Field(le=30, strict=True, ge=0)]] = Field(default=0, description="The number of days for which the full checksum from source Artifactory is trusted. Useful for migration process. The value cannot be more than 30 days. The default is 0.", alias="daysToTrust")
    __properties: ClassVar[List[str]] = ["checkBinaryExistenceAllowed", "daysToTrust"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ChecksumReplicationConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ChecksumReplicationConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "checkBinaryExistenceAllowed": obj.get("checkBinaryExistenceAllowed"),
            "daysToTrust": obj.get("daysToTrust") if obj.get("daysToTrust") is not None else 0
        })
        return _obj


