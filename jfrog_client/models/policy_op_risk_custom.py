# coding: utf-8

"""
    JFrog Platform API (Unofficial)

    **Unofficial** combined REST API spec for the JFrog Platform. This is not an official JFrog specification and may be incomplete or inaccurate.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PolicyOpRiskCustom(BaseModel):
    """
    Custom operational risk criteria. Use use_and_condition or use_or_condition to combine multiple conditions. 
    """ # noqa: E501
    op_risk_min_risk: Optional[StrictStr] = Field(default=None, description="Minimum operational risk level.")
    use_and_condition: Optional[StrictBool] = Field(default=None, description="When true, all custom conditions must be met (AND logic). When false, any single condition triggers the rule (OR logic). ")
    is_eol: Optional[StrictBool] = Field(default=None, description="Match packages that have reached end of life.")
    commits_less_than: Optional[StrictInt] = Field(default=None, description="Match packages with fewer commits per year than this value.")
    newer_versions_greater_than: Optional[StrictInt] = Field(default=None, description="Match packages where newer versions exceed this count.")
    committers_less_than: Optional[StrictInt] = Field(default=None, description="Match packages with fewer committers per year than this value.")
    release_date_greater_than_months: Optional[StrictInt] = Field(default=None, description="Match packages with a release date older than this many months.")
    release_cadence_per_year_less_than: Optional[StrictInt] = Field(default=None, description="Match packages with fewer releases per year than this value.")
    risk: Optional[StrictStr] = Field(default=None, description="Minimum operational risk level.")
    __properties: ClassVar[List[str]] = ["op_risk_min_risk", "use_and_condition", "is_eol", "commits_less_than", "newer_versions_greater_than", "committers_less_than", "release_date_greater_than_months", "release_cadence_per_year_less_than", "risk"]

    @field_validator('op_risk_min_risk')
    def op_risk_min_risk_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['high', 'medium', 'low']):
            raise ValueError("must be one of enum values ('high', 'medium', 'low')")
        return value

    @field_validator('commits_less_than')
    def commits_less_than_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([10, 25, 50, 100]):
            raise ValueError("must be one of enum values (10, 25, 50, 100)")
        return value

    @field_validator('newer_versions_greater_than')
    def newer_versions_greater_than_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 2, 3, 4, 5]):
            raise ValueError("must be one of enum values (1, 2, 3, 4, 5)")
        return value

    @field_validator('committers_less_than')
    def committers_less_than_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 2, 3, 4, 5]):
            raise ValueError("must be one of enum values (1, 2, 3, 4, 5)")
        return value

    @field_validator('release_date_greater_than_months')
    def release_date_greater_than_months_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([6, 12, 18, 24, 30, 36]):
            raise ValueError("must be one of enum values (6, 12, 18, 24, 30, 36)")
        return value

    @field_validator('release_cadence_per_year_less_than')
    def release_cadence_per_year_less_than_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 2, 3, 4, 5]):
            raise ValueError("must be one of enum values (1, 2, 3, 4, 5)")
        return value

    @field_validator('risk')
    def risk_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['high', 'medium', 'low']):
            raise ValueError("must be one of enum values ('high', 'medium', 'low')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PolicyOpRiskCustom from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PolicyOpRiskCustom from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "op_risk_min_risk": obj.get("op_risk_min_risk"),
            "use_and_condition": obj.get("use_and_condition"),
            "is_eol": obj.get("is_eol"),
            "commits_less_than": obj.get("commits_less_than"),
            "newer_versions_greater_than": obj.get("newer_versions_greater_than"),
            "committers_less_than": obj.get("committers_less_than"),
            "release_date_greater_than_months": obj.get("release_date_greater_than_months"),
            "release_cadence_per_year_less_than": obj.get("release_cadence_per_year_less_than"),
            "risk": obj.get("risk")
        })
        return _obj


